const mock = {
    defaultResponse: {
        body: {
            invalidFormat: "support will be removed",
        },
    },
};

module.exports = JSON.stringify(mock); // Deprecated and Invalid approach which will be removed in later versions of this package
